import React from 'react';

const AboutView = (props) => (
  <div id="about-view" style={{ height: '100%', width: '100%'}}>
    <img src='/peq.jpg' style={{ height: '100%', width: '100%'}} />
  </div>
);

export default AboutView;
